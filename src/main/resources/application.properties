# Configuraci\u00f3n de la base de datos
spring.datasource.url=jdbc:mysql://localhost:3306/gamestockapp?serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=898989
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Configuraci\u00f3n del servidor
server.port=9090

# Configuración SSL para HTTPS
server.ssl.enabled=true
server.ssl.key-store=classpath:gamestock-keystore.p12
server.ssl.key-store-password=898989
server.ssl.key-store-type=PKCS12
server.ssl.key-alias=gamestockssl

# Hibernate / JPA
spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Logging (opcional)
logging.level.org.springframework=INFO